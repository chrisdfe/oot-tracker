import type { FetchedImage } from './FetchedImage';

export type GreatFairyFountain = {
  number: string;
  location: string;
  conditions: string;
  reward: string;
  directions: string;
  images: FetchedImage[];
};

